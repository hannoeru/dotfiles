source ~/.profile
# Some OSes like Ubuntu/Mac expect bash_profile
# Redirect to bashrc
if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi

# Environment setup
if [ -f ~/.env ]; then
    source ~/.env
fi

fisource /etc/bash_completion.d/azds
